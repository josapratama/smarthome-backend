# Server
PORT=3000
NODE_ENV=development

# Database
# For local dev without Docker, set host/port accordingly
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/smarthome?schema=public"

# MQTT (optional, if you later add a publisher/consumer service)
MQTT_URL="mqtt://localhost:1883"
MQTT_USERNAME=""
MQTT_PASSWORD=""
